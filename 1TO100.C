#include <stdio.h>
#include<conio.h>
#include <stdlib.h>
void main()
{
		int n = 0;
	void foo()
	{
		void* x;
	 d
	}
		printf("%d\n", ++n);
	if (n >= 100)
	{
		exit(0);
	}


		getch();
}